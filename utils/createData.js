
export function createPurchaseData(id, title, price, rating) {
  return {
    id,
    title,
    price,
    rating,
  };
}

export function createInventoryData(id, title, price, rating) {
  return {
    id,
    title,
    price,
    rating,
  };
}
